# План работ

Итерация = одна пятичасовка лимита Claude Pro. В конце каждой итерации в папке лежит рабочий билд.

Статус обновляется в этом файле в конце каждой итерации.

---

## Итерация 1. Фундамент (текущая)

- [x] Клонирован и разобран прототип: vanilla JS, один файл 268 КБ, 8 маршрутов, ~230 функций
- [x] `CLAUDE.md` — правила проекта
- [x] `docs/ARCHITECTURE.md` — стек и слои
- [x] `docs/DESIGN.md` — дизайн-система, лечение однотипных плашек
- [x] `docs/FEATURES.md` — беклог с честной оценкой источников данных
- [x] Каркас Vite + React + TS + Tailwind v4, токены перенесены
- [x] Слой данных: клиенты OpenDota и Stratz, TanStack Query, IndexedDB-кэш
- [x] Прокси `/api/stratz`, `.env.local` с ключом, `.gitignore`
- [x] Базовая дизайн-система: 4 уровня поверхности, Tooltip на Floating UI, Skeleton, Stat, Bar
- [x] Роутинг, шапка, поиск, пустые страницы всех 8 маршрутов
- [x] Vitest настроен, первый тест на форматтерах
- [x] `npm run build` зелёный

## Итерация 2. Данные и страница игрока

- [x] `docs/SCORING.md` — формула оценки и четырёх граней, до кода
- [x] Страница игрока, первая версия: шапка, блок формы, лучшие герои, список матчей
- [x] Догрузка матчей по 20 при скролле через IntersectionObserver
- [x] Футер страницы игрока: лучшие герои (пункт 8.1)
- [x] Страница поиска: игроки и герои, пометка закрытых профилей
- [x] Скриншот-проверка в headless Chromium с подменой API на фикстуры
- [x] Web Worker для агрегаций по 500+ матчам
- [x] Страница игрока: три волны загрузки, скелетоны по форме контента
- [x] Бенчмарки по героям против медианы OpenDota (GPM, XPM) — раздел «Против медианы»
- [x] Метрики по ролям — раздел «По ролям» на странице игрока, роль берётся из Stratz (`position` на матче), 4 корзины (сейф/мид/офф/поддержка = позиции 4+5). Без доступа к Stratz раздел честно показывает «нужен доступ», эвристика по фарму/стороне не писалась (нет исходной логики прототипа в репозитории, это было бы решение заново, не перенос)
- [x] График активности за год (сделано в итерации 1)
- [x] Тренд формы: скользящий винрейт по 10 играм — `docs/DATAVIZ.md` написан, `WinrateTrend.tsx` подключён на странице игрока между «Формой» и графиком активности
- [x] Тултипы `math` на всех метриках блока «Форма»

## Итерация 3. Страница матча

- [x] Разбор матча: шапка (счёт, длительность, режим, статус разбора), ростер Radiant/Dire, 6 слотов предметов на игрока (тир нейтралки не сделан — у нейтральных предметов нет тира в стандартных `/constants/items`, нужен отдельный источник, не начато)
- [x] Ползунок времени, закреплённый внизу экрана (`TimeSlider.tsx`)
- [x] Предметы и метрики (GPM/ЛХ/XP) под ползунком — берутся из `gold_t/xp_t/lh_t`, предмет прячется, если куплен позже выбранной минуты. Полноценная 0-100 оценка под ползунком НЕ сделана: `docs/SCORING.md` требует референсные кривые по роли/бракету по минутам, их сбор — отдельная офлайн-задача (скрипт по OpenDota, не меньше 400 наблюдений на пару роль-минута), не начата. Решил не подделывать оценку числом без опоры — ровно как с трендом формы и `DATAVIZ.md` в итерации 2
- [x] Таланты в стиле Dota — два варианта на тир, выбранный подсвечен `--warm` (`TalentTree.tsx`, `talents.ts`). Прокачка обычных способностей собирается, но не отрисована отдельным треком — заведено в `talents.ts` (`build.skills`), но в UI это пока раздел талантов; трек обычных способностей по уровням можно добавить отдельно
- [x] Движок текстового разбора — `features/match/facts.ts`, набор правил по проверенным полям (первая кровь, лучший KDA, нетворс, урон, вышки, Рошан, эффективность линии, камбэк); без придуманных полей — там, где нужного поля в типах не было (участие в файтах по времени, урон и хилы по минутам), правило не писал
- [x] Поворотные моменты на таймлайне — `timeline.ts::findTurningPoints`, две категории: смена лидера по золоту и резкий отрыв (порог 3000 золота за минуту), отмечены на графике `GoldTimeline.tsx`
- [x] Экспорт — ссылка (копирование URL) и картинка (`html-to-image`, PNG всего блока матча кроме нижнего ползунка)

## Итерация 4. Герои и мета

- [x] Страница героя: предметы по фазам старт/ранняя/средняя/поздняя игра (`heroItemPopularity`) - честно подписано «популярность, не винрейт», потому что эндпоинт отдаёт только частоту покупки, не результат матчей с этим предметом
- [x] Матчапы - `heroMatchups`, топ-5 сильнее/слабее с порогом минимум 20 игр в выборке (`MIN_MATCHUP_GAMES`), чтобы не показывать шум на паре игр
- [x] Винрейт по длительности матча - `heroDurations`, полоса на бракет длительности, цвет по шкале `winrateColor` из `DATAVIZ.md`
- [x] Страница меты: тир-лист героев по бракету ранга (1-8, подписи медалей рекрут-титан) и отдельно про-сцена (`heroStats`, уже был типизирован), сортировка по винрейту
- [x] Meta hero grids - сетка карточек героя с винрейтом на странице меты
- [ ] Скиллбилд с винрейтом, фильтр меты по позиции, синергии героев, «кого учить дальше», парсер патчей - не начаты, см. ниже

Не сделано и почему:
- Порядок прокачки способностей с винрейтом - нужен ещё один нестабильный эндпоинт Stratz поверх уже неподтверждённого `PlayerRoleMatches`, решил не наслаивать два непроверенных источника подряд
- Фильтр меты по позиции - `heroStats` не разбивает пик/винрейт по позиции, только по бракету ранга и про-сцене; для позиции нужен другой источник данных, не найден
- Синергии героев (парные винрейты) - подтверждённого источника в OpenDota REST не нашёл
- «Кого учить дальше» - персонализация под пул игрока, это отдельное дизайн-решение, не мелкая правка
- Парсер чейнджлога патчей - как и в `FEATURES.md`, только если найдётся стабильный структурированный источник

Важная оговорка: живая проверка `heroMatchups`, `heroDurations`, `heroItemPopularity` в этой сессии не удалась - OpenDota стабильно отдавал 429 на все попытки. Формы ответа взяты из документированной схемы OpenDota (`hero_id/games_played/wins`, `duration_bin/games_played/wins`, объект по 4 фазам с `{itemId: count}`), не подтверждены вживую. Нужно проверить через `npm run dev` на реальных данных - если поля называются иначе, компоненты просто покажут пустое состояние, а не упадут (все три места защищены `?? []`/`?? {}`), но подписи и сортировка будут неверными до правки типов.

## Итерация 5. Драфт

- [x] Драфт-борд: 5 пиков Radiant, 5 пиков Dire, баны, переключатель режима клика (Radiant / Dire / Бан)
- [x] Контрпики с винрейтом - для каждой стороны считается средний винрейт уже выбранных вражеских героев против кандидата (`heroMatchups` пачкой на все выбранные герои), порог `MIN_MATCHUP_GAMES` тот же, что на странице героя
- [x] Банлист-советчик - топ меты по бракету/про-сцене (переиспользован тир-лист из `features/meta`), уже выбранные и забаненные герои исключены
- [x] Timing windows - среднее по `/durations` выбранных героев на сторону, честно подписано «не прогноз матча»
- [ ] Оценка драфта числом (лейн/файты/лейт), персональные рекомендации под свой пул - не сделаны, см. ниже

Не сделано и почему:
- Числовая оценка драфта по фазам - по своему же правилу проекта («метод описывается в `docs/SCORING.md` до кода») это отдельная методология, которую нельзя придумать по ходу написания компонента; нужен отдельный заход с расчётом на реальных данных, не в этой итерации
- Персональные рекомендации под свой пул - в `FEATURES.md` прямо написано «байесовское сглаживание на малой выборке обязательно», это отдельная статистическая задача, не мелкая правка

## Итерация 6. Киберспорт и сравнение

- [x] Сравнение двух игроков: метрики бок о бок за период, общие герои (от 3 игр у каждого), общие матчи (пересечение последних 500 игр каждого) - `/compare/:a/:b`, подбор игроков через поиск на самой странице
- [x] Страницы команд: состав, пул героев, последние матчи - `/team/:id`, добавлен эндпоинт `teamHeroes`
- [x] Лента про-матчей - `/esports`, `proMatches`
- [x] Лайв-матчи - список со счётом и длительностью, БЕЗ оценки вероятности победы
- [ ] Календарь турниров, heatmap перемещений, признаки смурфа - не сделаны, см. ниже

Не сделано и почему:
- Оценка вероятности победы на лайв-матчах - `FEATURES.md` сам относит это к P2 с оговоркой «обязательно подписываем, что это оценка»; я не стал придумывать формулу по ходу написания компонента, это в точности тот случай, который правило про честность и просит не делать
- Календарь турниров - нет подтверждённого источника расписания в OpenDota REST (`/leagues` даёт список лиг, не календарь с датами); лента про-матчей это частично закрывает
- Heatmap перемещений - работает только для распарсенных матчей, нужен отдельный экран с явным пустым состоянием под неразобранные; не начат
- Признаки смурфа - в `FEATURES.md` P3, честно как «признаки, не ярлык»; отдельная эвристика, не начата

Важная оговорка: `team`, `teamMatches`, `teamPlayers`, `teamHeroes`, `proMatches`, `live` не проверены вживую в этой сессии (тот же 429 от OpenDota). Поля взяты из документированной схемы, разложены по типам с большим количеством `| null`, чтобы не падать на неожиданной форме ответа. Проверь через `npm run dev`.

## Итерация 7. Полировка

- [x] Pre-flight по `CLAUDE.md`: нашёл и убрал 6 живых em-dash в видимых текстах (`facts.ts`, `GoldTimeline.tsx`, `MatchPage.tsx`, `RoleSplit.tsx`) - остались только из старых итераций, не были пойманы раньше; проверил скругления и акцент - без отклонений; декоративных точек перед пунктами нет по всему `src/`
- [x] Мобильный проход (приоритет 3, без фанатизма): шапка на узких экранах теряла пункты меню и кнопку профиля за краем экрана без возможности до них добраться - нав сделан горизонтально прокручиваемым, «Мой профиль» на узких экранах схлопывается до иконки
- [x] Проход по состояниям loading/empty/error - все экраны, собранные в итерациях 3-6, уже писались с четырьмя состояниями; отдельных пропусков при повторном просмотре не нашёл
- [ ] Производительность (бюджет 5 сек на холодную страницу игрока) - в этом контейнере нет доступа к живому OpenDota, поэтому холодную загрузку с реальной сетью замерить нельзя; собранный бандл 476 КБ (145 КБ gzip), в норме для SPA такого размера, но это не то же самое, что измеренное время
- [x] Деплой на Vercel - `vercel.json` уже настроен (framework vite, `npm run build`, rewrite на `index.html`); инструкция по заливке заказчиком в `README.md`/этом файле не менялась, специальных шагов под новые страницы не требуется, они статические маршруты того же SPA

---

## Бюджет и модель

Реалистично 7-9 итераций до состояния «заметно лучше прототипа». Дедлайна нет (ответ 11.1), поэтому темп определяется лимитом.

Переключение модели по фазам (согласовано в 12.2):

| Фаза | Модель | Effort |
|---|---|---|
| Ресерч, архитектура, дизайн-система, формула оценки | Opus 5 | High |
| Массовое написание компонентов, перенос функций, правки | Sonnet 5 | Medium |
| Чтение файлов, поиск, механические замены | Haiku 4.5 | Low |

Внутри одной сессии модель фиксирована настройкой приложения. Переключаю то, чем управляю: подагентов на объёмных механических задачах и глубину рассуждений. Если предстоит итерация целиком из механической работы, скажу перед стартом, чтобы переключить на Sonnet вручную.

## Как проверяется работоспособность

В облачном контейнере нет доступа к api.opendota.com, поэтому живые данные там не проверить. Вместо этого:

1. `npm run build` и `npm run test` обязаны быть зелёными.
2. Сборка поднимается в headless Chromium, запросы к OpenDota подменяются фикстурами (`scripts/shot-mocked.mjs`), снимается скриншот и читаются ошибки консоли.
3. Проверка на живых данных — на твоей машине через `npm run dev`.

Так уже поймана одна ошибка: кэш в IndexedDB падал с `DataCloneError`, потому что в дамп попадали ещё не завершённые запросы. Исправлено в `src/main.tsx`.

Вторая пойманная так ошибка, после итераций 5-7: три пары файлов в проекте различались только регистром первой буквы при одинаковом остальном имени (`BanSuggestions.tsx`/`banSuggestions.ts`, `TimingWindows.tsx`/`timingWindows.ts`, а также более старая `ItemSlots.tsx`/`itemSlots.ts` из итерации 3). На Linux-контейнере, где идёт сборка и тесты в этой среде, это два разных файла, и `tsc`/`vite build` проходят чисто. На Windows файловая система не различает регистр в имени файла, поэтому `tsc` увидел коллизию и билд упал с `TS1149`/`TS1261`. Правило на будущее: не называть логику и компонент в одной папке именами, различающимися только регистром - используется тот же приём, что уже был в `features/hero` и `features/meta` (`matchups.ts` + `HeroMatchups.tsx`, `tierlist.ts` + `HeroGrid.tsx` - разные по смыслу имена, не только по регистру). Файлы переименованы: `banSuggestions.ts` → `bans.ts`, `timingWindows.ts` → `timing.ts`, `itemSlots.ts` → `itemSlotFields.ts`. Отдельно всё ещё нужно было `npm install` после итерации 3 - `html-to-image` был добавлен в `package.json`, но не попал в уже установленный `node_modules` на твоей машине.

## Известные ограничения этой среды

- На компьютере не поднялась локальная Linux-среда, поэтому команды там выполнять нельзя. Сборка и тесты идут в облачном контейнере, готовые файлы кладутся в папку `Vantage2`.
- Для локального запуска на компьютере нужен Node.js 20+. Проверить: `node -v` в PowerShell. Если нет — https://nodejs.org, LTS.
- Заливка на GitHub — руками (ответ 9.1).
